# Raw camera files — huge and unused by the site (web-ready .jpg versions are committed instead)
images/*.CR2
images/*.cr2

# Local tooling config
.claude/

# OS cruft
.DS_Store

# Local scratch/log output
*.log

# Sponsor source decks — 28MB of Canva export; the banners are extracted to
# images/sponsors/ and committed there instead
Sponser Slide*.pptx
Sponser Slide*.pdf
